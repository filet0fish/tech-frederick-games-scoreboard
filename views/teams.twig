{% extends 'layout.twig' %}

{% block body %}
<h1>{{team.name}}</h1>
  <ul>
    {% for game in games %}

         <li>{{game.name}} Score:{{game.score}}</li>

     {% endfor %}
    <li>Team Total Score:{{team.score}}</li>
  </ul>
<a href="..">Score Board</a>
{% endblock %}
